import { ComponentFixture, TestBed } from '@angular/core/testing';
import { EditarclasemodalPage } from './editarclasemodal.page';

describe('EditarclasemodalPage', () => {
  let component: EditarclasemodalPage;
  let fixture: ComponentFixture<EditarclasemodalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(EditarclasemodalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
