import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CrearclasemodalPage } from './crearclasemodal.page';

describe('CrearclasemodalPage', () => {
  let component: CrearclasemodalPage;
  let fixture: ComponentFixture<CrearclasemodalPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CrearclasemodalPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
